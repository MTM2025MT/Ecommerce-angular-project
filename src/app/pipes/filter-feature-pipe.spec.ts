import { FilterFeaturePipe } from './filter-feature-pipe';

describe('FilterFeaturePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFeaturePipe();
    expect(pipe).toBeTruthy();
  });
});
