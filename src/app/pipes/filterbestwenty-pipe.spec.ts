import { FilterbestwentyPipe } from './filterbestwenty-pipe';

describe('FilterbestwentyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbestwentyPipe();
    expect(pipe).toBeTruthy();
  });
});
