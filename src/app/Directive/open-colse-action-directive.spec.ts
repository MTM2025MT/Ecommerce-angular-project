import { OpenColseActionDirective } from './open-colse-action-directive';

describe('OpenColseActionDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenColseActionDirective();
    expect(directive).toBeTruthy();
  });
});
