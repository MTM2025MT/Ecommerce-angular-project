import { AppClickToggle } from './app-click-toggle';

describe('AppClickToggle', () => {
  it('should create an instance', () => {
    const directive = new AppClickToggle();
    expect(directive).toBeTruthy();
  });
});
